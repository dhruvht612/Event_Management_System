/**
 * Single source of truth for landing page horizontal layout.
 * max-w-7xl (1280px) + consistent padding — use for hero, stats, CTA, footer.
 */
export const landingContainer =
  'mx-auto w-full max-w-7xl px-5 sm:px-6 lg:px-8'

/** Inner horizontal padding for large contained cards (stats, CTA shell). */
export const landingCardPadding = 'px-8 py-12 sm:px-10 md:px-12 lg:px-14 lg:py-14 xl:px-16'
